FROM node:22-bookworm-slim

ENV NODE_ENV=production
WORKDIR /app

RUN npm install --global @openai/codex

COPY package.json package-lock.json ./
RUN npm ci --omit=dev

COPY . .

USER node
CMD ["node", "--experimental-strip-types", "scripts/markets-worker.ts"]
